# تصميم واجهة ANTER Messenger

## هدف المنتج

ANTER Messenger هو تطبيق Android مستقل للمراسلة الخاصة داخل شبكة ANTER. يقدّم تجربة عربية أولاً، سريعة وواضحة للاستخدام بيد واحدة، ويعيد استخدام هوية المستخدم وعلاقات المتابعة وقواعد الخصوصية في ANTER. لا يحفظ التطبيق محتوى الصوت داخل سجل المكالمات؛ بل يعرض حالات المكالمة ووقتها ومدتها فقط.

## مبادئ الواجهة

يعتمد التطبيق وضعاً داكناً هادئاً مستلهماً من هوية ANTER: كحلي عميق للخلفية، أزرق سماوي للتفاعل الأساسي، وأخضر محدود لحالات الاتصال والنجاح. صُممت كل شاشة على مقاس هاتف عمودي بنسبة 9:16، مع إجراءات أساسية في الجزء السفلي ضمن مدى إبهام اليد الواحدة. تستخدم البطاقات زوايا دائرية متوسطة، ونصاً عربياً عالي التباين، وحالات تحميل أو أخطاء صريحة بدلاً من أزرار غير فعالة بلا تفسير.

| عنصر الهوية | الاختيار |
|---|---|
| الخلفية | `#071526` كحلي ليلي |
| السطح والبطاقات | `#10233B` أزرق كحلي متدرج |
| اللون الأساسي | `#52A7FF` أزرق ANTER |
| النجاح والاتصال | `#38C98A` أخضر اتصال |
| النص الأساسي | `#F3F8FF` أبيض مزرق |
| النص الثانوي | `#AABBD2` رمادي أزرق |

## قائمة الشاشات

| الشاشة | المحتوى والوظائف الأساسية |
|---|---|
| ترحيب وربط الحساب | شرح موجز للتطبيق وربط حساب ANTER عند اكتمال واجهة الربط الآمنة. في النسخة الأولية تُعرض حالة الربط التجريبية فقط من دون طلب كلمة مرور داخل التطبيق. |
| صندوق المحادثات | قائمة محادثات مرتبة زمنياً، صورة الحساب، حالة الاتصال، آخر رسالة، عداد غير المقروء، وزر بدء محادثة جديد. |
| المحادثة | رسائل نصية، حقل كتابة مثبت أسفل الشاشة، إرسال، رد سريع، نسخ، حالة الكتابة، بطاقات سجل المكالمات، ومعاودة اتصال. |
| شاشة اتصال وارد أو جارٍ | اسم وصورة الطرف الآخر، حالة الرنين أو الاتصال، أزرار قبول ورفض وإنهاء، ومدة المكالمة بعد القبول. |
| جهات الاتصال | حسابات ANTER التي توجد متابعة متبادلة معها فقط، مع بحث محلي وزر بدء محادثة. |
| الإشعارات | المكالمات الفائتة والرسائل الجديدة وحالات الاتصال، مع فتح المحادثة المقصودة بالنقر. |
| الإعدادات | حالة الحساب، أذونات الإشعارات والميكروفون، المظهر، رابط الرجوع إلى ANTER، وتسجيل الخروج عند اكتمال الربط. |

## التدفقات الرئيسة

| التدفق | الخطوات |
|---|---|
| فتح محادثة | صندوق المحادثات ← اختيار محادثة ← قراءة الرسائل ← كتابة رسالة ← إرسال ← ظهور حالة الإرسال. |
| بدء محادثة جديدة | جهات الاتصال ← اختيار حساب متابَع متبادل ← فتح المحادثة ← إرسال أول رسالة. |
| استقبال مكالمة | إشعار وارد ← فتح التطبيق أو إشعار النظام ← شاشة الرنين ← قبول أو رفض ← شاشة الاتصال ← إنهاء ← بطاقة سجل المكالمة داخل المحادثة. |
| معاودة الاتصال | بطاقة سجل المكالمة ← معاودة الاتصال ← التحقق من المتابعة المتبادلة ← بدء الرنين. |
| تعليم الخصوصية | الإعدادات ← شرح أن المراسلة والاتصال يتطلبان متابعة متبادلة وأن سجل المكالمات لا يتضمن الصوت. |

## نطاق الإصدار الأول

يركز الإصدار الأول على تجربة Android محلية مكتملة الواجهة: صندوق محادثات، محادثة مفصلة، جهات اتصال مؤهلة، سجل مكالمات، واجهة رنين، وإشعارات محلية تجريبية. يتم تجهيز طبقة بيانات موحدة لتوصيلها لاحقاً بمسارات API مؤمنة في ANTER. الربط الفعلي مع حسابات ANTER سيحتاج إضافة API مخصص للمصادقة والرسائل من الخادم؛ لا يُنقل ملف الجلسة أو كلمة المرور إلى التطبيق.
